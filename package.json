{
  "name": "meiguodizhionline",
  "version": "1.0.0",
  "private": true,
  "description": "Static address generator site for Cloudflare Pages.",
  "scripts": {
    "build": "node scripts/generate-country-pages.mjs && node scripts/build.mjs",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
